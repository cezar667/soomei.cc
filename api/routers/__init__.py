"""
FastAPI routers grouped por domínio (auth, cards, admin, etc.).

Each file inside this package should expose an APIRouter that can be included in
the main application (app.py). This keeps endpoint definitions close to their
use cases and avoids the single mega-file.
"""

